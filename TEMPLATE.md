# Project Name

One sentence describing what this is, in plain words. No "AI-powered," no "next-generation," no "blazingly fast" unless you have benchmarks.

## The problem

One or two sentences about why this exists. What pain were you in when you wrote this?

## Quick example

A code block, terminal session, or output snippet that shows the tool working. Skip the screenshot if a code block tells the story faster.

```
$ tool input
expected output
```

## Install

```
npm install your-package
```

If installation is more involved (Docker, multiple steps), link to a dedicated install doc.

## Use it

The minimum viable usage. One example, runnable as-written. Add more under "More examples" if needed, but the first one should work without modification.

```
import { thing } from 'your-package';

const result = thing({ option: true });
console.log(result);
```

## API reference

If the API is more than a function or two, link out to dedicated docs:

- [Full API reference](docs/API.md)
- [Configuration options](docs/CONFIG.md)

If it's small, document it inline:

### `thing(options)`

- `options.x` (string, required) — what x is
- `options.y` (number, optional, default `0`) — what y is
- Returns: a `Result` object with `.value` and `.success`

## What it doesn't do

Set expectations honestly. Three to six bullets. This section earns trust.

- Does NOT do X (which the name might imply)
- Does NOT support Y (yet, or by design)
- Does NOT replace Z (and isn't trying to)

## Contributing

See [CONTRIBUTING.md](CONTRIBUTING.md). Or if you don't have one yet:

> Issues and PRs welcome. Run `npm test` before submitting. For larger changes, open an issue first to discuss the approach.

## Tech

What's it built with, what does it need? One line is fine.

> Node 18+, no runtime dependencies, ~500 lines of source.

## License

MIT. See [LICENSE](LICENSE).

## Related

- [project-a](https://github.com/your/project-a) — what it does
- [project-b](https://github.com/your/project-b) — what it does

## Acknowledgments

Optional. Credit prior art, inspirations, or specific contributors.
